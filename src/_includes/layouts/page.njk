---
layout: layouts/base.njk
---
<article class="page">
  <h1>{{ title }}</h1>
  {% if image %}
    <img class="page__hero" src="{{ image | url }}" alt="{{ title }}">
  {% endif %}
  <div class="page__body">
    {{ content | safe }}
  </div>
</article>
