<header class="site-header">
  <a class="site-header__logo" href="{{ '/' | url }}">
    {% if site.logo %}
      <img src="{{ site.logo | url }}" alt="{{ site.site_name | default('Logo') }}">
    {% else %}
      <span>{{ site.site_name | default("Site name") }}</span>
    {% endif %}
  </a>
  {% include "partials/nav.njk" %}
</header>
