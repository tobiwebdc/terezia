{
  "name": "cms-core",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Eleventy + Decap CMS core layer, synced into per-client repos.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "debug": "DEBUG=Eleventy* eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "luxon": "^3.7.2"
  }
}
