{
  "name": "cms-core",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Eleventy + Decap CMS core layer, synced into per-client repos.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "debug": "DEBUG=Eleventy* eleventy",
    "css-watch": "npx @tailwindcss/cli -i ./src/assets/css/tailwind.css -o ./src/assets/css/base.css --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "luxon": "^3.7.2",
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
